DATABASE_URL=postgresql://postgres:password@localhost:5432/baseball_seats
# Railway 內部 PostgreSQL 通常不需設定。若使用要求 TLS 的外部資料庫再改為 true。
PGSSL=false
# 修改既有座位（取消／換位／換桌）需要的確認密碼。未設定時預設為 870821。
SEAT_EDIT_PASSWORD=870821
